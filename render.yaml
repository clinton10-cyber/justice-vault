services:
  - type: web
    name: securevault-pro
    runtime: python
    plan: free
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: SECRET_KEY
        generateValue: true
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /
